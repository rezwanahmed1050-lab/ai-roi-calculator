[build]
  command = "cd artifacts/universal-roi-calculator && pnpm install && BASE_PATH=/ pnpm run build"
  publish = "artifacts/universal-roi-calculator/out"

[dev]
  command = "cd artifacts/universal-roi-calculator && pnpm run dev"
